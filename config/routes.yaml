admin:
  resource: '../src/Admin/Infrastructure/Routes/'
  type:     directory

exam-api:
  resource: '../src/Exam/Infrastructure/config/'
  type:     directory

telegram:
  resource: '../src/Telegram/Infrastructure/config/'
  type:     directory

app.swagger_ui:
  path: /api/doc
  methods: GET
  defaults: { _controller: nelmio_api_doc.controller.swagger_ui }

app.swagger:
  path: /api/doc.json
  methods: GET
  defaults: { _controller: nelmio_api_doc.controller.swagger }